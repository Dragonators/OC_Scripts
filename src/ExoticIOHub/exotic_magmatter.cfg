-- Install as /home/exotic_magmatter.cfg and replace every address prefix.
local sides = require("sides")
return {
  ioHubAddress = "CHANGE_ME_IOHUB",
  inputHatchSide = sides.east, -- direction from IO Hub to the adjacent ninefold input hatch
  databaseAddress = "CHANGE_ME_DATABASE",
  gtMachineAddress = "CHANGE_ME_GT_MACHINE",
  gpuAddress = nil,
  screenAddress = nil,

  cpuName = "Exotic Plasma",

  pollInterval = 0.2,
  craftPollInterval = 0.5,
  craftTimeout = 600,
  dispatchTimeout = 30,
  recipeTimeout = 900,
  partialHintTimeout = 30,
  maxCraftAttempts = 6,
  stableSamples = 1,
  recycleQuietSeconds = 1, -- wait once, then rescan the whole IO Hub

  screenWidth = 120,
  screenHeight = 35,
  logLines = 6,
  journalPath = "/var/exotic_magmatter.state",

  databaseSlots = {
    fluidCellStart = 1, -- slots 1..3 are used in this mode
    craftDrop = 8,
    plasmaProbe = 9
  }
}
