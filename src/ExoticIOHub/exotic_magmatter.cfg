-- Install as /home/exotic_magmatter.cfg and replace every address prefix.
return {
  ioHubAddress = "CHANGE_ME_IOHUB",
  dualHatchAddress = "CHANGE_ME_DUALHATCH",
  databaseAddress = "CHANGE_ME_DATABASE",
  gtMachineAddress = "CHANGE_ME_GT_MACHINE",
  gpuAddress = nil,
  screenAddress = nil,

  cpuName = "Exotic Plasma",

  pollInterval = 0.5,
  craftPollInterval = 2,
  craftTimeout = 600,
  dispatchTimeout = 30,
  recipeTimeout = 900,
  partialHintTimeout = 30,
  maxCraftAttempts = 6,
  stableSamples = 2,
  recycleQuietSamples = 6, -- one whole-IO quiet window; not a delay for every slot

  screenWidth = 120,
  screenHeight = 35,
  logLines = 6,
  journalPath = "/var/exotic_magmatter.state",

  databaseSlots = {
    fluidCellStart = 1, -- slots 1..3 are used in this mode
    craftDrop = 8,
    plasmaProbe = 9
  }
}
