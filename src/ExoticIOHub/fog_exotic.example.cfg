return {
  -- 每台 OC 电脑控制一台模块：quark 或 magmatter。
  mode = "quark",

  components = {
    -- 留空仅适用于同类型组件恰好只有一个的情况；有多个时填写唯一地址前缀。
    transposer = "",
    database = "",
    meInterface = "",
    gtMachine = "",
    gpu = "",

    -- 二合一 ME 接口在 OC 复合驱动下的主组件名。
    meInterfaceType = "me_interface",
  },

  sides = {
    -- 转运器朝 GT 模块缓存输入仓/输入仓的方向。
    cache = "east",
    -- 转运器朝“二合一 ME 接口”的方向。提示物和补给流体都只走这里。
    main = "down",
  },

  -- 可选：只允许使用指定名称的 AE CPU；留空则由 AE 自动选择。
  aeCpuName = "",

  timing = {
    poll = 0.25,
    stableScans = 2,
    interfaceWarmup = 0.75,
    interfaceClearTimeout = 8,
    returnStallTimeout = 10,
    missingPatternRetry = 5,
    craftPoll = 0.25,
    craftWarningAfter = 600,
    interfaceSupplyGrace = 2,
    transferYield = 0.05,
    returnVerifyDelay = 0.1,
    -- 补给后多久仍未观察到下一轮完整提示时，在 GUI 中给出警告。
    advanceWarningAfter = 30,
    -- 下一轮完整提示本身就是机器推进信号，无需额外静置。
    cycleSettle = 0,
  },

  gui = {
    width = 80,
    height = 25,
  },
}
