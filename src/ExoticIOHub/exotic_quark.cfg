-- Install as /home/exotic_quark.cfg and replace every address prefix.
local sides = require("sides")
return {
  interfaceAddress = "CHANGE_ME_ME_INTERFACE",
  transposerAddress = "CHANGE_ME_TRANSPOSER",
  interfaceSide = sides.down, -- direction from the transposer to the ME dual fluid interface
  inputHatchSide = sides.east, -- direction from the transposer to the advanced storage input hatch
  databaseAddress = "CHANGE_ME_DATABASE",
  gtMachineAddress = "CHANGE_ME_GT_MACHINE",
  gpuAddress = nil,       -- nil is allowed only when exactly one GPU exists
  screenAddress = nil,    -- nil is allowed only when exactly one screen exists

  cpuName = "Exotic Plasma",

  pollInterval = 0.2,
  craftPollInterval = 0.5,
  craftTimeout = 600,
  interfaceFillTimeout = 15,
  dispatchTimeout = 30,
  recipeTimeout = 900,
  partialHintTimeout = 30,
  maxCraftAttempts = 6,
  stableSamples = 1,
  recycleQuietSeconds = 1, -- wait once, then rescan the whole input hatch

  screenWidth = 120,
  screenHeight = 35,
  logLines = 6,
  journalPath = "/var/exotic_quark.state",

  databaseSlots = {
    fluidDropStart = 1, -- slots 1..7 are regenerated AE2FC droplets for the interface filter
    craftDrop = 8,      -- temporary AE2FC droplet used for pattern lookup
    plasmaProbe = 9     -- temporary GT plasma-cell metadata probe
  }
}
